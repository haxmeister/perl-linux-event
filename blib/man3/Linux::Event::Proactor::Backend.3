.\" -*- mode: troff; coding: utf-8 -*-
.\" Automatically generated by Pod::Man v6.0.2 (Pod::Simple 3.47)
.\"
.\" Standard preamble:
.\" ========================================================================
.de Sp \" Vertical space (when we can't use .PP)
.if t .sp .5v
.if n .sp
..
.de Vb \" Begin verbatim text
.ft CW
.nf
.ne \\$1
..
.de Ve \" End verbatim text
.ft R
.fi
..
.\" \*(C` and \*(C' are quotes in nroff, nothing in troff, for use with C<>.
.ie n \{\
.    ds C` ""
.    ds C' ""
'br\}
.el\{\
.    ds C`
.    ds C'
'br\}
.\"
.\" Escape single quotes in literal strings from groff's Unicode transform.
.ie \n(.g .ds Aq \(aq
.el       .ds Aq '
.\"
.\" If the F register is >0, we'll generate index entries on stderr for
.\" titles (.TH), headers (.SH), subsections (.SS), items (.Ip), and index
.\" entries marked with X<> in POD.  Of course, you'll have to process the
.\" output yourself in some meaningful fashion.
.\"
.\" Avoid warning from groff about undefined register 'F'.
.de IX
..
.nr rF 0
.if \n(.g .if rF .nr rF 1
.if (\n(rF:(\n(.g==0)) \{\
.    if \nF \{\
.        de IX
.        tm Index:\\$1\t\\n%\t"\\$2"
..
.        if !\nF==2 \{\
.            nr % 0
.            nr F 2
.        \}
.    \}
.\}
.rr rF
.\"
.\" Required to disable full justification in groff 1.23.0.
.if n .ds AD l
.\" ========================================================================
.\"
.IX Title "Linux::Event::Proactor::Backend 3"
.TH Linux::Event::Proactor::Backend 3 2026-03-09 "perl v5.42.0" "User Contributed Perl Documentation"
.\" For nroff, turn off justification.  Always turn off hyphenation; it makes
.\" way too many mistakes in technical documents.
.if n .ad l
.nh
.SH NAME
Linux::Event::Proactor::Backend \- Contract for completion backends used by Linux::Event::Proactor
.SH DESCRIPTION
.IX Header "DESCRIPTION"
This module documents the contract implemented by completion backends for
Linux::Event::Proactor.
.PP
A proactor backend submits operations to an underlying completion mechanism,
tracks backend tokens, delivers raw completions, and participates in
cancellation. The proactor engine itself owns operation objects, callback
queueing, result normalization, and settle\-once guarantees.
.SH "REQUIRED METHODS"
.IX Header "REQUIRED METHODS"
.SS _new(%args)
.IX Subsection "_new(%args)"
Construct the backend. The \f(CW\*(C`loop\*(C'\fR argument is required.
.SS \fBname()\fP
.IX Subsection "name()"
Return a short backend name such as \f(CW\*(C`uring\*(C'\fR or \f(CW\*(C`fake\*(C'\fR.
.ie n .SS "_submit_read($op, %args)"
.el .SS "_submit_read($op, \f(CW%args\fP)"
.IX Subsection "_submit_read($op, %args)"
.ie n .SS "_submit_write($op, %args)"
.el .SS "_submit_write($op, \f(CW%args\fP)"
.IX Subsection "_submit_write($op, %args)"
.ie n .SS "_submit_recv($op, %args)"
.el .SS "_submit_recv($op, \f(CW%args\fP)"
.IX Subsection "_submit_recv($op, %args)"
.ie n .SS "_submit_send($op, %args)"
.el .SS "_submit_send($op, \f(CW%args\fP)"
.IX Subsection "_submit_send($op, %args)"
.ie n .SS "_submit_accept($op, %args)"
.el .SS "_submit_accept($op, \f(CW%args\fP)"
.IX Subsection "_submit_accept($op, %args)"
.ie n .SS "_submit_connect($op, %args)"
.el .SS "_submit_connect($op, \f(CW%args\fP)"
.IX Subsection "_submit_connect($op, %args)"
.ie n .SS "_submit_timeout($op, %args)"
.el .SS "_submit_timeout($op, \f(CW%args\fP)"
.IX Subsection "_submit_timeout($op, %args)"
.ie n .SS "_submit_shutdown($op, %args)"
.el .SS "_submit_shutdown($op, \f(CW%args\fP)"
.IX Subsection "_submit_shutdown($op, %args)"
.ie n .SS "_submit_close($op, %args)"
.el .SS "_submit_close($op, \f(CW%args\fP)"
.IX Subsection "_submit_close($op, %args)"
Submit the operation and return a backend token suitable for registry and
cancellation bookkeeping.
.SS _cancel_op($op)
.IX Subsection "_cancel_op($op)"
Attempt to cancel the in\-flight operation. Cancellation may complete
asynchronously. The backend must cooperate with the proactor\*(Aqs settle\-once and
registry rules.
.SS \fB_complete_backend_events()\fP
.IX Subsection "_complete_backend_events()"
Drive backend completion processing once and return the number of processed
backend events when available.
.SH "COMPLETION RULES"
.IX Header "COMPLETION RULES"
A backend must never run user callbacks inline. It should report raw completion
facts back into the owning proactor so the proactor can normalize the result and
queue any callback.
.PP
For io_uring\-style backends, a negative completion result is a negative errno.
The backend or the engine must normalize that into a positive errno and create a
Linux::Event::Error object.
.SH "BUFFER LIFETIME"
.IX Header "BUFFER LIFETIME"
For operations that expose user buffers to the kernel, the backend must keep the
necessary Perl values alive until the kernel no longer needs them.
.SH "CANCELLATION AND RACES"
.IX Header "CANCELLATION AND RACES"
Cancellation is inherently racy. A backend must be written so that an operation
cannot settle twice even when completion and cancellation race closely together.
.SH "SEE ALSO"
.IX Header "SEE ALSO"
Linux::Event::Proactor,
Linux::Event::Proactor::Backend::Uring,
Linux::Event::Proactor::Backend::Fake,
Linux::Event::Operation,
Linux::Event::Error
